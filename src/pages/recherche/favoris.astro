---
// src/pages/recherche/favoris.astro
import Navbar from '../../components/Navbar.astro';
import '../../styles/main.css';
---

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Favoris & Historique - Maths974</title>
</head>
<body>
  
  <Navbar title="Favoris" />

  <main class="page-container">
    
    <header class="page-header">
      <h1>Mes Favoris & Historique</h1>
      <p>Retrouvez vos fiches sauvegardées et vos dernières consultations.</p>
    </header>
    
    <div class="tabs">
      <button class="tab-btn active" data-tab="favoris">
        ⭐ Favoris <span class="tab-badge" id="favorisCount">0</span>
      </button>
      <button class="tab-btn" data-tab="historique">
        🕐 Historique <span class="tab-badge" id="historiqueCount">0</span>
      </button>
      <a href="/recherche/" class="tab-btn" style="text-decoration:none;">
        🔍 Recherche
      </a>
    </div>
    
    <div class="tab-panel active" id="panel-favoris">
      <div class="actions-bar">
        <span class="stats-count"><strong id="favorisTotal">0</strong> éléments</span>
        <button class="action-btn danger" id="clearFavoris">🗑️ Vider les favoris</button>
      </div>
      
      <div class="results-grid" id="favorisList"></div>
      
      <div class="empty-state" id="emptyFavoris">
        <span class="empty-icon">⭐</span>
        <h2 class="empty-title">Aucun favori pour le moment</h2>
        <p class="empty-text">Ajoutez des fiches en favoris lors de votre navigation.</p>
        <a href="/recherche/" class="btn-cta">Parcourir les fiches</a>
      </div>
    </div>
    
    <div class="tab-panel" id="panel-historique">
      <div class="actions-bar">
        <span class="stats-count"><strong id="historiqueTotal">0</strong> consultés récemment</span>
        <button class="action-btn danger" id="clearHistorique">🗑️ Effacer l'historique</button>
      </div>
      
      <div class="results-grid" id="historiqueList"></div>
      
      <div class="empty-state" id="emptyHistorique">
        <span class="empty-icon">🕐</span>
        <h2 class="empty-title">Historique vide</h2>
        <p class="empty-text">Vos dernières consultations apparaîtront ici.</p>
        <a href="/" class="btn-cta">Aller à l'accueil</a>
      </div>
    </div>
    
  </main>
  
  <script type="module">
    import { Favoris, Historique } from '/src/utils/storage.js';

    // UI Logic
    const tabs = document.querySelectorAll('.tab-btn[data-tab]');
    const panels = document.querySelectorAll('.tab-panel');

    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const target = tab.dataset.tab;
        tabs.forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
        panels.forEach(p => p.classList.remove('active'));
        document.getElementById(`panel-${target}`).classList.add('active');
      });
    });

    // Helper functions
    function formatDate(isoDate) {
      if(!isoDate) return '';
      const date = new Date(isoDate);
      return date.toLocaleDateString('fr-FR', { day: 'numeric', month: 'short' });
    }

    function createCard(etiq, showRemove = true, dateField = null) {
      const card = document.createElement('a');
      card.href = etiq.url;
      card.className = 'result-card'; // Using unified class
      
      let removeBtnHTML = '';
      if (showRemove) {
        removeBtnHTML = `<button class="card-remove" data-remove="${etiq.slug}" title="Retirer">×</button>`;
      }
      
      const dateHTML = (dateField && etiq[dateField]) 
        ? `<div style="font-size:0.8em; color:#64748b; margin-top:5px;">Vu le ${formatDate(etiq[dateField])}</div>` 
        : '';

      card.innerHTML = `
        ${removeBtnHTML}
        <div class="card-header">
           ${etiq.id ? `<span class="card-id">${etiq.id}</span>` : ''}
        </div>
        <h3 class="card-title">${etiq.title}</h3>
        <div class="card-meta">
          ${etiq.niveau ? `<span class="meta-badge">📚 ${etiq.niveau}</span>` : ''}
          ${etiq.theme ? `<span class="meta-badge">📂 ${etiq.theme}</span>` : ''}
        </div>
        ${dateHTML}
      `;
      return card;
    }

    // Logic for Favoris
    function displayFavoris() {
      const favoris = Favoris.getAll();
      const container = document.getElementById('favorisList');
      const empty = document.getElementById('emptyFavoris');
      
      document.getElementById('favorisCount').textContent = favoris.length;
      document.getElementById('favorisTotal').textContent = favoris.length;
      
      container.innerHTML = '';
      if (favoris.length === 0) {
        empty.style.display = 'block';
        return;
      }
      empty.style.display = 'none';
      
      favoris.forEach(fav => {
        const card = createCard(fav, true, 'addedAt');
        container.appendChild(card);
        
        // Handle click on remove button (stop propagation to link)
        const btn = card.querySelector('[data-remove]');
        if(btn) {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                e.stopPropagation();
                Favoris.remove(fav.slug);
                displayFavoris();
            });
        }
      });
    }

    // Logic for Historique
    function displayHistorique() {
      const hist = Historique.getAll();
      // Note: ID changed in HTML to match JS logic standard (historiqueList vs historiquList typo in original)
      const container = document.getElementById('historiqueList'); 
      const empty = document.getElementById('emptyHistorique');
      
      document.getElementById('historiqueCount').textContent = hist.length;
      document.getElementById('historiqueTotal').textContent = hist.length;
      
      container.innerHTML = '';
      if (hist.length === 0) {
        empty.style.display = 'block';
        return;
      }
      empty.style.display = 'none';
      
      hist.forEach(item => {
        const card = createCard(item, false, 'visitedAt');
        container.appendChild(card);
      });
    }

    document.getElementById('clearFavoris').addEventListener('click', () => {
      if(confirm('Tout supprimer ?')) { Favoris.clear(); displayFavoris(); }
    });
    
    document.getElementById('clearHistorique').addEventListener('click', () => {
      if(confirm('Tout effacer ?')) { Historique.clear(); displayHistorique(); }
    });

    // Init
    displayFavoris();
    displayHistorique();
  </script>
</body>
</html>