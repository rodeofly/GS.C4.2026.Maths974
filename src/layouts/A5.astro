---
import BasePrintLayout from './BasePrintLayout.astro';

const { info, niveau, theme, sousTheme } = Astro.props;
const fm = info?.data ?? info?.frontmatter ?? {};

// 🛠️ CORRECTION : Priorité au titre passé en prop, puis au frontmatter
const title = propsTitle ?? fm.title ?? fm.sous_theme ?? sousTheme ?? 'Sans titre';
const themeLabel = fm.theme ?? theme ?? (path.includes('/rapidos/') ? 'Rapidos' : 'Général');
const niveauLabel = fm.niveau ?? niveau ?? '';
---

<BasePrintLayout
  info={info}
  title={title}
  layoutClass="layout-a5"
  niveauLabel={niveauLabel}
  themeLabel={themeLabel}
  indexContent={indexContent}
>
  <slot />
  <slot name="footer" slot="footer" />
</BasePrintLayout>